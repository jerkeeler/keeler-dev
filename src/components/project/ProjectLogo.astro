---
import { Image } from 'astro:assets';

export interface Props {
  img: ImageMetadata;
  alt: string;
}
const { img, alt } = Astro.props;
---

<Image src={img} alt={alt} width={100} class="w-12 h-12 mr-6" />
