---
export interface Props {
  type: 'button' | 'submit' | 'reset';
}
const { type } = Astro.props;
---

<button type={type} class="shadow bg-primary text-white rounded-lg w-24 p-2 hover:bg-primary-dark">
  <slot />
</button>
