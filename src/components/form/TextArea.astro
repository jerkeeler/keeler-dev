---
export interface Props {
  name: string;
  placeholder: string;
  required?: boolean;
}
const { name, placeholder, required } = Astro.props;
---

<textarea
  name={name}
  class="shadow border-solid border border-gray-300 rounded-lg p-2 focus:shadow-outline max-w-2xl"
  rows={6}
  placeholder={placeholder}
  required={required}
  min-length={10}
>
</textarea>
