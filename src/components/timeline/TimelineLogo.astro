---
import { Image } from 'astro:assets';

export interface Props {
  img: ImageMetadata;
  alt: string;
}
const { img, alt } = Astro.props;
---

<Image class="w-16 h-16" src={img} alt={alt} width={100} />
